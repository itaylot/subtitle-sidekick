# TO-DO — Subtitle Sidekick

מסמך עבודה מרוכז. מחר אפשר פשוט להצביע על סעיף ולהגיד "בצע את X".
מקורות: ה-Release Readiness Audit (סבבים 1-6) + רעיונות אישיים.

**מגבלות קשיחות (לא לשבור):** בלי endpoint/API key מוטמע, בלי שרת משותף (כל משתמש על
RunPod שלו), מצב מקומי נשאר ברירת מחדל ופרטי, בלי DB/תלות/abstraction חדשים אלא אם
חובה, פשטות ובר-הסבר לפני הכול, commits בלי `Co-Authored-By`.

**מקרא עדיפויות:** P0 = חוסם הפצה / אובדן מידע / קריסה. P1 = חשוב לפני הקפאה.
P2 = שיפור קטן בעל ערך. Future = אחרי המבחנים. Remove = למחוק/לפשט.

---

## ✅ בוצע כבר

- [x] **סבב 1 — בטיחות נתונים** (commit `5ff0374`): הרצאות שהקובץ שלהן לא נגיש נשמרות
  ומסומנות `missing` (לא נמחקות); כתיבת JSON אטומית לכל הקבצים; `.bak` מתגלגל
  (מתרענן רק מקובץ תקין); שמירת `.corrupt`/`.corrupt.N` לפני החזרת מצב ריק.
- [x] **מסך Library** (commit `5018e36`): רשת קורסים + מסך פירוט + חיפוש בקורס.
- [x] **חיצים ← → בנגן = דילוג 10 שניות** (לא דקה) — הטמעתי היום (הנגן הנייטיב כבר לא
  "בולע" את החיצים; ה-handler שלנו תמיד מטפל בהם). *לבדוק בנגן האמיתי.*
- [x] **תפריט ⋯ שנפתח מחוץ למסך** — היום: התפריט מתהפך כלפי מעלה כשאין מקום למטה
  (חל גם על תפריט הכתוביות). *לבדוק על הרצאה בתחתית מגירת הקורסים.*

---

## סבב 2 — נכונות תור וענן (P0/P1, ~חצי שעה) — הבא בתור המומלץ

- [ ] **STAB-1 (P0)** · ביטול/סגירה בזמן תמלול ענן לא מבטל את ה-jobs ב-RunPod →
  ממשיך לחייב. תיקון: `POST /cancel/{id}` fire-and-forget בנתיב הביטול.
  `cloud_backend.py:268-301`, `+ finally אחרי abort.set()`.
- [ ] **STAB-2 (P0)** · reentrancy ב-`processNext`: `processing=true` נקבע רק אחרי
  `await` → שני תמלולים במקביל. תיקון: להעביר את הדגל לראש הפונקציה. `ui/app.js`.
- [ ] **STAB-3 (P0)** · debounce של 300ms מאבד סטטוס "הושלם" → תמלול חוזר של הרצאה
  שלמה אחרי סגירה. תיקון: שמירה מיידית במעברי סטטוס. `ui/app.js:430-435`.
- [ ] **STAB-4 (P1)** · `api.start` לא נתפס → תור קפוא בשקט. `.catch(...)`. שורה אחת.
- [ ] **STAB-5 (P1)** · שחזור תור בעלייה דורס תור חי. merge במקום החלפה. `ui/app.js:360`.

## סבב 3 — התקנה ועדכון (P0/P1, ~חצי שעה)

- [ ] **DIST-1 (P0)** · לנעוץ גרסאות ב-`requirements.txt` (הגרסאות שעובדות אצלי כרגע):
  `faster-whisper==1.2.1`, `pywebview==6.2.1`, `python-docx==1.2.0`,
  `yt-dlp==2026.6.9`, `requests==2.34.2`. שקול `>=` ל-yt-dlp (נשבר כש-YouTube משתנה).
- [ ] **DIST-2 (P1)** · `run.bat`: `if errorlevel 1` אחרי pip; שרשרת fallback לגרסת
  Python (`py -3.12 || py -3.11 || py -3`).
- [ ] **DIST-3 (P1)** · `update.bat`: להזיז `taskkill` לאחרי הורדה מוצלחת; `if
  errorlevel 8` אחרי robocopy; לסרב לעדכן אם תמלול רץ.

## סבב 4 — UX (P1, ~שעה)

- [ ] **UX-1 (P1)** · שגיאות Python גולמיות באנגלית מוצגות למשתמש → למפות 3-4 מחלקות
  כשל (אימות/רשת/דיסק) להודעות עברית ב-`app.py`; הטקסט הגולמי נשאר ב-crash.log.
- [ ] **UX-2 (P1)** · ביטול תמלול עדיין ב-`confirm()` דפדפני → `confirmModal`. שורה.
- [ ] **UX-3 (P1)** · הורדת מודל ראשונה בלי גודל → להוסיף "(כ-1.5GB)". `app.js:771`.
- [ ] **UX-4 (P2)** · דיוק המדריך: הערת "three modes" ישנה, "דילוג 10 שניות" שייך
  לנגן הישן, לא מזכיר את מסך Library. `index.html`.
- [ ] **SEC-1 (P1)** · שרת המדיה מגיש כל קובץ עם `Access-Control-Allow-Origin: *` →
  טוקן אקראי ב-`media_url`, או להסיר את כותרת ה-CORS. `app.py:79-98`. *(לבדוק שה-origin
  של pywebview לא צריך את הכותרת.)*

## סבב 5 — ניקיון ותיעוד (P2/Remove, ~שעה)

- [ ] **RM-1 (Remove)** · נגן מותאם-אישית מת (~130-150 שורות, `NATIVE_PLAYER=true`
  קבוע) — למחוק `.ctrls`/handlers/CSS. **זהירות**: `timeupdate` מכיל קוד חי (resume,
  הדגשת תמליל) — למחוק רק כתיבות `subline`/`pfill`/`tc`.
- [ ] **RM-2 (Remove)** · CSS יתום: `.appic`, `.homeic`, `.fast/.sw/.knob` (~21 שורות).
- [ ] **RM-3 (Remove)** · שלושה עותקים של יצירת-קורס-inline → helper אחד (~30 שורות).
- [ ] **DOCS-1 (P1)** · להוסיף `LICENSE` (MIT) + פסקת קרדיטים למודלים (ivrit-ai,
  Whisper/OpenAI, faster-whisper, yt-dlp).
- [ ] **DOCS-2 (P2)** · badge Python 3.12 מול requirements 3.11/3.12; שורת activate
  מעורבבת Win/mac ב-README.
- [ ] **TEST-1 (P2)** · להרחיב את `__main__` הקיים: asserts ל-`_version_tuple`
  (הבאג `"1.10" < "1.9"`) ול-`fmt_time`. בלי framework.
- [ ] **PRIV-1 (P2)** · crash.log רושם URL מלא של הורדות (טוקני session) → hostname
  בלבד; + משפט ב-README על `cookiesfrombrowser`.

## סבב 6 — Release Candidate

- [ ] רשימת בדיקות ידניות מלאה (ראו הדוח: התקנה, תמלול מקומי/ענן, ביטול, נתונים,
  עדכון, מילון). תיוג `v1.1.0` + push.

---

## רעיונות אישיים (להוסיף/לתעדף)

- [ ] **סמן כנצפה / כלא-נצפה** — כפתור בהרצאה שמחליף `viewed`. דורש bridge קטן
  (`set_viewed(video, bool)` ב-engine+app.py) + כפתור ב-`renderLecRow`/תפריט ⋯.
  קטן-בינוני. *(P2)*
- [ ] **אנימציות מעבר בין עמודים** — transition על החלפת `.view`. עיצובי, לא טריוויאלי
  (צריך לתזמן hidden↔visible עם opacity/transform בלי לשבור את `show()`). *(P2/Future)*
- [ ] **אייקון לכל קורס** — שדה `icon` בכל קורס + בורר אייקונים. שינוי מודל נתונים
  + UI. בינוני. *(Future)*
- [ ] **מסך מלא אמיתי (לא "חלון בתוך חלון")** — כרגע `.win` הוא כרטיס ממורכז ברוחב 720px
  על רקע מנוקד. אפשרויות: (א) `webview.create_window(..., maximized=True)` +
  ל-`.win` לתת `max-width` גדול/מלא; (ב) fullscreen אמיתי. החלטת עיצוב — לדון לפני
  ביצוע (משנה את כל האסתטיקה). *(P2 — צריך החלטה)*
- [ ] **המשך מדויק בין הפעלות** — כבר קיים `saveResume`/כרטיס "המשך מאיפה שעצרת"
  (localStorage per-video). ההרחבה: בעלייה, לפתוח אוטומטית את ההרצאה האחרונה במיקום
  השמור (או לפחות למקד אותה). קטן. *(P2)*

---

## הערות שכדאי לזכור (מתוך distribution-plan)

- לקדם `version.txt` באותו commit של כל שינוי משתמש, אחרת העדכון לא יזוהה.
- אף פעם לא לקמט secrets — המפתח חי רק ב-`settings.json` תחת `Videos/Subtitle Sidekick`.
- נתוני המשתמש מחוץ לריפו — זו הסיבה שעדכונים בטוחים; לא להחזיר אותם פנימה.
- Docker + OneDrive: לבנות `runpod_server` מנתיב מחוץ ל-OneDrive.
